<ui:Page x:Class="MCBS.WpfApp.Pages.Home.EmptyInstancePage"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:lex="http://wpflocalizeextension.codeplex.com"
         lex:LocalizeDictionary.DesignCulture="zh-CN"
         lex:ResxLocalizationProvider.DefaultAssembly="MCBS.WpfApp"
         lex:ResxLocalizationProvider.DefaultDictionary="Lang"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:local="clr-namespace:MCBS.WpfApp.Pages.Home"
         xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
         mc:Ignorable="d" 
         d:DesignHeight="450" d:DesignWidth="800"
         Title="{lex:Loc Title_EmptyInstancePage}">

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="{lex:Loc EmptyInstancePage_NoSelectedInstance}"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"
                   MaxWidth="600"/>
            <TextBlock FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="600">
                <Run Text="{lex:Loc EmptyInstancePage_GuidancePrefix}"/>
                <Hyperlink Command="{Binding CreateInstanceCommand}">
                    <Run Text="{lex:Loc EmptyInstancePage_CreateInstanceLink}"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</ui:Page>
